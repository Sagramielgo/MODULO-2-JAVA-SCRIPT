'use strict';

const names = ['Rita', 'María', 'Lucía', 'Ana', 'Vanesa'];
console.log(names.slice(1, 3)); // ["María", "Lucía"]
console.log(names.slice(1)); // ['María', 'Lucía', 'Ana', 'Vanesa']

console.log(names.slice(-2)); // ['Ana', 'Vanesa']
